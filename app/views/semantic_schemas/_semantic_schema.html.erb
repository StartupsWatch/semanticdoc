<li>
  <%=link_to semantic_schema.name.capitalize, semantic_schema_path(semantic_schema)%>: <%=semantic_schema.properties.map {|p| p.name}.join(", ")%>
  <% if semantic_schema.has_children? %>
  <ul>
    <% semantic_schema.children.each do |sc| %>
      <%= render "semantic_schema", semantic_schema: sc %>
    <% end%>
  </ul>
  <% end %>
</li>